name: Deploy services

on:
  push:
    tags:
      - v*
    branches:
      - dev
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  BRANCH_NAME: ${{ github.base_ref || github.ref_name }}

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, ubuntu-24.04-arm]

    if: ${{ always() }}
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 'Short sha'
        id: vars_sha
        run: echo "short_sha=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: "Build Docker image: buang-api"
        uses: docker/build-push-action@v5
        env:
          TAG: ${{ case(env.BRANCH_NAME == 'master', 'latest', format('{0}-{1}', env.BRANCH_NAME, steps.vars_sha.outputs.short_sha) )}}
        with:
          context: "{{defaultContext}}:api"
          platforms: ${{ matrix.os == 'ubuntu-latest' && 'linux/amd64' || 'linux/arm64' }}
          push: true
          build-args: VERSION=${{ steps.vars_sha.outputs.short_sha }}
          tags: |
            skulpture/buang-api:${{ env.TAG }}-${{ matrix.os == 'ubuntu-latest' && 'amd64' || 'arm64' }}

  docker-push:
    needs: build

    strategy:
      matrix:
        image: [skulpture/buang-api]

    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: 'Short sha'
        id: vars_sha
        run: echo "short_sha=${GITHUB_SHA::7}" >> "$GITHUB_OUTPUT"

      - name: Push ${{ matrix.image }}
        uses: Noelware/docker-manifest-action@v1
        env:
          BRANCH_TAG: ${{ case(env.BRANCH_NAME == 'master', 'latest', env.BRANCH_NAME )}}
          TAG: ${{ case(env.BRANCH_NAME == 'master', 'latest', format('{0}-{1}', env.BRANCH_NAME, steps.vars_sha.outputs.short_sha) )}}
        with:
          inputs: >
            ${{ matrix.image }}:${{ env.TAG }}-amd64,
            ${{ matrix.image }}:${{ env.TAG }}-arm64
          tags: >-
            ${{ matrix.image }}:${{ env.TAG }},
            ${{ matrix.image }}:${{ env.BRANCH_TAG }}
          push: true
